{
  "name": "gitaru",
  "version": "1.0.0",
  "private": true,
  "description": "Gitaru local media utility",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test": "node --test tests"
  },
  "engines": {
    "node": ">=20"
  }
}
